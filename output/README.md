# output/

`make` writes the built ROM here, named after the IWAD you built it from
(e.g. `DOOM2.z64`).

Run it in an N64 emulator (e.g. Ares, simple64) or flash it to an
EverDrive-64. Nothing here is committed - these are build artifacts, rebuild
them any time from your own IWAD in `input/`.
